# Local Agent Instructions

## Delivery Requirement
- After every iteration where a result is presented to the user, always:
  1. Build the app.
  2. Copy/update the built `.app` to `~/Applications`.
- This is mandatory for every delivered iteration.
